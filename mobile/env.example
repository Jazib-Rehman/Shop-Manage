# Base URL of the Next.js app (no trailing /api — paths already include it)
# Web / simulator on same machine:
EXPO_PUBLIC_API_URL=http://localhost:3001
# Physical phone on same Wi‑Fi — use your computer’s LAN IP instead:
# EXPO_PUBLIC_API_URL=http://192.168.x.x:3001
